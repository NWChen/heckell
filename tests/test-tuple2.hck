let f : int * int -> bool;
f(x, y) = x < y end

let g : int -> real * int;
g(x) = (x/1.234, 2*x) end

let h : int * real * bool -> real * bool * int;

let t : int * (real -> real) * int;

let v : (int -> int) * (bool -> real * real) *int;
